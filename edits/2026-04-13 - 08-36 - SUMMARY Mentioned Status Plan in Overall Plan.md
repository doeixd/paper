Summary:

Added an explicit mention of the current status-plan note to the top of `overall.md.txt` so the main reframing plan now immediately points readers to the companion file that tracks implementation status.

What changed:

- Updated `latex/springer-sn-template/sn-article-template/claude-notes/overall.md.txt`.
- Added a short `Working companion note` block directly under the title.
- The new line points to `2026-04-13-rainforest-reframing-status-plan.md` and states its purpose:
  tracking what from the main plan is already implemented, partly done, or still outstanding.

Why:

- The earlier quick index already summarized the status-plan note, but it appeared lower in the document.
- Putting the reference at the top makes `overall.md.txt` function more clearly as the main entry point for both the strategic plan and the current implementation state.

Checks performed:

- Confirmed that the addition is brief, accurate, and does not duplicate the fuller quick-index summaries below.
